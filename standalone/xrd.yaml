apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition  # defines the schema and the user-facing API (1:1 with Composition)
metadata:
  name: xbuckets.platform.example.org
spec:
  group: platform.example.org
  names:
    kind: XBucket  # XRD maps user input (via spec fields) to the corresponding composite resource
    plural: xbuckets
  claimNames:
    kind: Bucket
    plural: buckets
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                bucketName:
                  type: string

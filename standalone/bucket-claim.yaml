apiVersion: platform.example.org/v1alpha1
kind: BucketClaim # Developer-facing
metadata:
  # 1. User creates one Claim: kind: Bucket
  # 2. Crossplane creates one XR: kind: XBucket
  # 3. Composition renders 2+ managed resources: Bucket, Policy, etc.
  # This is how Crossplane enables infra composition without the user needing to know what's inside.
  name: platform-bucket
spec:
  bucketName: platform-artifacts-1234
